{
  "name": "agentfw-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "agentfw — an AI agent firewall on the wire (monorepo root; the published package is @openguardrails/agentfw under packages/agentfw).",
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*", "internal/ui"],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "agentfw": "npm run agentfw -w @openguardrails/agentfw --",
    "daemon": "npm run daemon -w @openguardrails/agentfw --",
    "ui:dev": "npm run dev -w @openguardrails/ui",
    "ui:build": "npm run build -w @openguardrails/ui",
    "build": "npm run ui:build && npm run build -w @openguardrails/agentfw",
    "typecheck": "npm run typecheck -w @openguardrails/agentfw",
    "test": "npm run test -w @openguardrails/agentfw",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.0"
  }
}
